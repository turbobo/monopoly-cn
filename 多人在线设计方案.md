# 大富翁中国行 - 在线多人设计方案

## 一、当前架构分析

### 现有代码结构（共 2814 行）

| 文件 | 行数 | 职责 | 多人改造影响 |
|------|------|------|-------------|
| `page.tsx` | 1184 | UI层 + 游戏流程控制 | **大改**：新增房间系统 + 网络同步 |
| `game-engine.ts` | 519 | 纯逻辑（掷骰/移动/购买/破产） | **小改**：增加序列化/反序列化 |
| `board-renderer.ts` | 936 | Canvas棋盘渲染 | **不改** |
| `sound.ts` | 175 | Web Audio音效 | **不改** |

### 当前部署方式
- EdgeOne Pages 静态导出（`output: 'export'`）
- 纯前端，无后端服务
- 不消耗任何服务器计算资源和大模型 Token

### 核心约束
- EdgeOne Pages 只能托管静态文件，**不能运行 Node.js 后端**
- 必须全免费
- 回合制游戏，延迟容忍度高（1-2秒可接受）

---

## 二、技术选型（全免费）

### 方案：Supabase（推荐）

| 能力 | Supabase 免费版 | 是否够用 |
|------|----------------|---------|
| PostgreSQL 数据库 | 500MB | ✅ 游戏状态极小 |
| Realtime 实时推送 | 200并发连接 | ✅ 同时在线几十局足够 |
| Auth 匿名登录 | 5万月活 | ✅ 绰绰有余 |
| Edge Functions | 50万次/月 | ✅ 可选用 |
| API 调用 | 无限制 | ✅ |

**为什么选 Supabase：**
- 免费额度大，200并发对回合制游戏绰绰有余
- Realtime 基于 PostgreSQL LISTEN/NOTIFY，开箱即用
- 客户端 SDK 成熟（`@supabase/supabase-js`）
- 不需要改 Next.js 部署方式，前端仍走 EdgeOne Pages 静态导出

### 备选方案对比

| 方案 | 免费额度 | 优点 | 缺点 |
|------|---------|------|------|
| **Supabase** | 200并发/500MB | 全功能、成熟稳定 | 需要注册 |
| Firebase | 100并发/1GB | Google生态 | 国内访问不稳定 |
| Cloudflare Durable Objects | 10万次/天 | 边缘计算快 | 复杂度高、免费额度少 |
| 自建 WebSocket (Render) | 750小时/月 | 完全可控 | 会休眠、不稳定 |

---

## 三、架构设计

```
┌─────────────────────────────────────────────────┐
│                  玩家A浏览器                      │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │ page.tsx │  │  useRoom │  │ supabaseClient│  │
│  │  (UI)    │  │  (Hook)  │  │  (SDK)        │  │
│  └────┬─────┘  └────┬─────┘  └───────┬───────┘  │
│       │              │                │           │
└───────┼──────────────┼────────────────┼───────────┘
        │              │                │
        │    WebSocket (Supabase Realtime)
        │              │                │
┌───────┼──────────────┼────────────────┼───────────┐
│       │              │                │           │
│  ┌────▼──────────────▼────────────────▼────────┐  │
│  │              Supabase Cloud                  │  │
│  │  ┌─────────────┐  ┌──────────────────────┐  │  │
│  │  │  Realtime   │  │  PostgreSQL           │  │  │
│  │  │  (WebSocket)│  │  rooms / game_state   │  │  │
│  │  └─────────────┘  └──────────────────────┘  │  │
│  └─────────────────────────────────────────────┘  │
│                                                    │
│                  玩家B浏览器                        │
│  （同上结构，通过 Realtime 订阅同一房间频道）         │
└────────────────────────────────────────────────────┘
```

**数据流向：**
1. 玩家A掷骰子 → `page.tsx` 调用 `executeTurn()` 计算结果
2. 结果写入 Supabase `game_state` 表
3. Supabase Realtime 推送给同房间所有玩家
4. 玩家B/C/D 的 `useRoom` Hook 收到更新 → 更新本地 `game` state → Canvas重绘

---

## 四、数据库设计

### 表结构

```sql
-- 房间表
CREATE TABLE rooms (
  id          TEXT PRIMARY KEY,          -- 4位房间号，如 "A3K7"
  host_id     UUID REFERENCES auth.users(id),
  status      TEXT DEFAULT 'waiting',    -- waiting / playing / finished
  settings    JSONB DEFAULT '{}',        -- { initialMoney, difficulty, maxRounds }
  game_state  JSONB DEFAULT '{}',        -- 完整 GameState JSON
  created_at  TIMESTAMPTZ DEFAULT now(),
  updated_at  TIMESTAMPTZ DEFAULT now()
);

-- 房间玩家表
CREATE TABLE room_players (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  room_id     TEXT REFERENCES rooms(id) ON DELETE CASCADE,
  user_id     UUID REFERENCES auth.users(id),
  name        TEXT NOT NULL,
  avatar      TEXT DEFAULT '🧑',
  slot        INT NOT NULL,              -- 座位号 0-3
  is_ready    BOOLEAN DEFAULT false,
  created_at  TIMESTAMPTZ DEFAULT now()
);

-- 索引：快速查找房间和玩家
CREATE INDEX idx_room_players_room ON room_players(room_id);
CREATE INDEX idx_rooms_status ON rooms(status);
```

**为什么用 JSONB 存 game_state：**
- GameState 是一个完整的 JavaScript 对象，直接 JSON 存储最方便
- 每次操作更新整个 state，避免细粒度字段同步的复杂性
- 回合制游戏操作频率低（每回合一次），全量更新无性能问题

### RLS 安全策略

```sql
-- 只允许房间成员读写
ALTER TABLE rooms ENABLE ROW LEVEL SECURITY;
CREATE POLICY "room_access" ON rooms
  USING (id IN (SELECT room_id FROM room_players WHERE user_id = auth.uid()));

ALTER TABLE room_players ENABLE ROW LEVEL SECURITY;
CREATE POLICY "player_access" ON room_players
  USING (room_id IN (SELECT room_id FROM room_players WHERE user_id = auth.uid()));
```

---

## 五、核心流程设计

### 5.1 房间创建与加入

```
创建者                    Supabase                    加入者
  │                         │                          │
  │── createRoom() ────────>│                          │
  │   生成4位房间号          │                          │
  │   插入 rooms 表          │                          │
  │   插入 room_players 表   │                          │
  │<── 返回 room_id ────────│                          │
  │                         │                          │
  │   显示"等待玩家加入..."   │                          │
  │                         │<── joinRoom(code) ───────│
  │                         │   验证房间存在+未满        │
  │                         │   插入 room_players 表    │
  │                         │── Realtime 推送 ────────>│
  │<── Realtime 推送 ───────│   "新玩家加入"            │
  │                         │                          │
  │   所有人 ready → 房主点击"开始游戏"                   │
  │── startGame() ─────────>│                          │
  │   调用 createGame()      │                          │
  │   写入 game_state        │                          │
  │── Realtime 广播 ────────┼── 所有玩家收到 ──────────>│
```

### 5.2 回合执行（核心）

```
当前玩家                  Supabase                  其他玩家
  │                         │                        │
  │ 轮到自己（phase=roll）    │                        │
  │── rollDice() ──────────>│                        │
  │   本地计算 executeTurn()  │                        │
  │   更新 game_state        │                        │
  │                         │── Realtime 推送 ──────>│
  │                         │   新的 game_state       │
  │                         │                        │── 收到推送
  │                         │                        │   更新本地 state
  │                         │                        │   Canvas 重绘 + 音效
  │                         │                        │   如果轮到自己 → 操作
```

**关键设计：服务端不计算，客户端计算 + 同步**

因为 Supabase 免费版没有服务端计算能力，采用：
1. 当前玩家本地调用 `executeTurn()` 计算结果
2. 将完整的更新后 GameState 写入数据库
3. 其他玩家收到推送后直接应用新状态

**防作弊：** 回合制休闲游戏，作弊动机极低。如需防护可后续加 Edge Function 验证。

### 5.3 购买决策（需要等待响应）

```
当前玩家                  Supabase                  其他玩家
  │                         │                        │
  │ 到达无主地皮              │                        │
  │ phase 变为 'action'      │                        │
  │ 弹出"是否购买？"          │                        │
  │                         │                        │ 显示"等待XX决策..."
  │── 点击"购买" ───────────>│                        │
  │   buyProperty() 本地计算  │                        │
  │   更新 game_state        │                        │
  │── Realtime 广播 ────────┼── 收到新状态 ──────────>│
  │                         │   显示购买动画            │
```

### 5.4 拍卖流程（多人竞价）

```
拍卖发起                Supabase              其他玩家
  │                      │                     │
  │ 放弃购买→触发拍卖      │                     │
  │ 写入 auction_state    │                     │
  │── Realtime ─────────>┼── 推送 ────────────>│
  │                      │   "拍卖开始！"        │
  │                      │                     │
  │ 出价 ¥X              │                     │ 出价 ¥Y
  │── update bid ───────>│<── update bid ──────│
  │                      │                     │
  │                      │ 所有非当前出价者       │
  │                      │ 5秒无人加价→结束       │
  │                      │ 更新 game_state      │
  │<── Realtime ─────────┼── 推送结果 ────────>│
```

---

## 六、新增代码文件

### 6.1 `src/lib/supabase-client.ts`（~30行）

```typescript
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!

export const supabase = createClient(supabaseUrl, supabaseAnonKey)
```

### 6.2 `src/hooks/useRoom.ts`（~200行）

核心 Hook，管理房间状态和网络同步：

```typescript
// 关键状态
interface RoomState {
  roomId: string | null           // 当前房间号
  status: 'idle' | 'waiting' | 'playing' | 'finished'
  players: RoomPlayer[]           // 房间内玩家列表
  mySlot: number                  // 自己的座位号
  game: GameState | null          // 游戏状态（从Supabase同步）
  messages: string[]              // 消息日志
}

// 核心函数
createRoom(settings): Promise<string>     // 创建房间，返回房间号
joinRoom(code: string): Promise<void>     // 加入房间
startOnlineGame(): Promise<void>          // 开始游戏
syncGameState(gs: GameState): Promise<void> // 同步游戏状态到数据库
subscribeToRoom(): void                   // 监听房间状态变化
leaveRoom(): void                         // 离开房间

// Realtime 监听
supabase.channel(`room:${roomId}`)
  .on('postgres_changes', { event: 'UPDATE', schema: 'public', table: 'rooms' },
    (payload) => {
      // 收到新的 game_state → 更新本地状态
      const newState = payload.new.game_state as GameState
      setGame(newState)
      // 触发棋盘动画和音效
    }
  )
  .subscribe()
```

### 6.3 `src/lib/room-utils.ts`（~50行）

```typescript
// 生成4位房间号（大写字母+数字，排除易混淆字符）
export function generateRoomCode(): string

// 将 GameState 序列化为 JSONB（处理 Map/Set 等特殊类型）
export function serializeGameState(gs: GameState): object

// 从 JSONB 反序列化为 GameState
export function deserializeGameState(data: object): GameState
```

---

## 七、现有文件改造

### 7.1 `page.tsx` 改造（改动量：中等）

**新增 Screen 状态：**

```typescript
type Screen = 'menu' | 'setup' | 'room' | 'game' | 'end'
//                ↑ 新增：房间大厅（创建/加入/等待）
```

**新增 GameMode：**

```typescript
type GameMode = 'ai' | 'local' | 'online'
//                               ↑ 新增：在线多人
```

**新增 UI 区域：**

```
房间大厅（Screen = 'room'）
├── 创建房间按钮 → 生成房间号 → 显示等待界面
├── 加入房间 → 输入4位房间号 → 加入
└── 等待室
    ├── 已加入玩家列表（头像+名字+准备状态）
    ├── "准备" 按钮
    └── 房主可见："开始游戏" 按钮（所有人ready后激活）
```

**handleRoll 改造逻辑：**

```typescript
// 原来的 handleRoll（本地模式）
const handleRoll = () => {
  const gs = { ...game! }
  const dice = rollDice()
  const msgs = executeTurn(gs, dice)
  setGame(gs)
  // ... 处理消息、音效等
}

// 在线模式的 handleRoll
const handleRollOnline = async () => {
  const gs = { ...game! }
  const dice = rollDice()
  const msgs = executeTurn(gs, dice)
  setGame(gs)
  // 同步到数据库
  await syncGameState(gs)
  // ... 处理消息、音效等（与本地一致）
}
```

**判断是否轮到自己：**

```typescript
const isMyTurn = mode !== 'online' || game.currentPlayer === mySlot
// 非自己回合时，禁用掷骰子/购买按钮
```

### 7.2 `game-engine.ts` 改造（改动量：极小）

```typescript
// 新增：Player 增加在线标识
export interface Player {
  // ... 现有字段不变
  userId?: string    // 在线模式下的 Supabase user id
  isOnline?: boolean // 是否在线玩家（区别于AI和本地玩家）
}

// GameState 不需要改，已有的结构完全可以序列化
```

### 7.3 `board-renderer.ts` — 不改

Canvas 渲染器完全不感知网络状态，它只接收 `Player[]` 数据绘制。

### 7.4 `sound.ts` — 不改

音效播放由 `page.tsx` 的 `triggerBoardEffects` 控制，收到 Realtime 推送后同样触发。

---

## 八、断线重连设计

```typescript
// useRoom.ts 中
useEffect(() => {
  // 监听网络状态
  const handleOnline = () => {
    // 重新订阅 Realtime channel
    subscribeToRoom()
    // 从数据库拉取最新 game_state
    fetchLatestState()
  }

  window.addEventListener('online', handleOnline)
  return () => window.removeEventListener('online', handleOnline)
}, [roomId])

// 断线期间的操作
// 回合制游戏容忍度高：断线重连后拉取最新状态即可
// 如果断线时轮到自己：显示"重新连接中..."，超时30秒自动跳过
```

---

## 九、用户体验流程

```
打开游戏
├── 本地多人 → 现有流程不变
├── 挑战AI → 现有流程不变
└── 🌐 在线对战（新增）
    ├── 创建房间
    │   ├── 设置：初始资金 + 难度
    │   ├── 显示4位房间号
    │   ├── 等待其他玩家加入（显示已加入列表）
    │   ├── 所有人准备 → 房主点击"开始"
    │   └── 进入游戏
    ├── 加入房间
    │   ├── 输入4位房间号
    │   ├── 进入等待室
    │   ├── 点击"准备"
    │   └── 房主开始 → 进入游戏
    └── 快速匹配（v2考虑）
        ├── 自动寻找未满房间
        └── 无可用房间 → 自动创建
```

---

## 十、部署与配置

### 环境变量

```env
# .env.local
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs...
```

### EdgeOne Pages 部署

不需要改动。EdgeOne Pages 仍然做静态导出，Supabase 作为外部 BaaS 服务从客户端直接调用。

```json
// edgeone.json（不变）
{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "nodeVersion": "20.18.0",
  "framework": "nextjs"
}
```

### 新增依赖

```bash
npm install @supabase/supabase-js
```

仅此一个包，增加约 30KB（gzip 后）。

---

## 十一、Supabase 免费额度风险评估

| 指标 | 免费版上限 | 大富翁预估用量 | 风险 |
|------|-----------|--------------|------|
| 数据库大小 | 500MB | <1MB（JSONB很小） | ✅ 无风险 |
| Realtime并发 | 200连接 | 每局4人，可同时50局 | ✅ 无风险 |
| Realtime消息/月 | 200万 | 每局约500消息×100局=5万/月 | ✅ 无风险 |
| Auth月活 | 5万 | 匿名登录，极低 | ✅ 无风险 |
| API请求/月 | 无限制 | 极低 | ✅ 无风险 |
| 数据库带宽 | 2GB/月 | JSONB很小，<100MB | ✅ 无风险 |

**结论：免费额度完全够用，即使日活1000用户也没有压力。**

---

## 十二、开发计划

| 阶段 | 内容 | 预估工作量 | 产出 |
|------|------|-----------|------|
| **P0 - 基础联机** | Supabase配置 + useRoom Hook + 创建/加入房间 + 状态同步 | 4-6小时 | 可在线对战 |
| **P1 - 体验优化** | 断线重连 + 超时跳过 + 等待动画 + 房间列表 | 2-3小时 | 体验流畅 |
| **P2 - 社交功能** | 快速匹配 + 游戏内聊天 + 战绩统计 | 3-4小时 | 可分享传播 |

**总计：约 10-13 小时开发量，分 3 个迭代完成。**

建议先做 P0，跑通核心流程后再迭代 P1/P2。

---

*设计版本：v1.0*
*日期：2026年6月*
